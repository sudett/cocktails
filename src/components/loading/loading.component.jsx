import React from "react";

import { LoaderContainer, Loader } from "./loading.styles";

const Loading = () => (
  <LoaderContainer>
    <Loader></Loader>
  </LoaderContainer>
);

export default Loading;
